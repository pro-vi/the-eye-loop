.prov/
.gnhf/
.env
node_modules/
.claude/
.svelte-kit/
.vercel/

# Research (cloned repos, syntheses, internal findings)
.research/

# scripts/ is ignored by default (local experiments, boss-run logs); keep the
# live validator and search-set entrypoints tracked, but treat their generated
# artifacts as local run outputs.
scripts/*
!scripts/validate.sh
!scripts/validate.mjs
!scripts/search-set.sh
!scripts/search-set.mjs
scripts/findings/

benchmarks/runs/

debug.jsonl
